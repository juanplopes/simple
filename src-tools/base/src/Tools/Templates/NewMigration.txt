using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Simple.Migrator.Framework;
using Simple.Migrator.Fluent;
using System.Data;

namespace ${opt.Namespace}.Tools.Migrations
{
    [Migration(${timestamp})]
    public class Migration${timestamp} : FluentMigration
    {
        public override void Up()
        {
            /* try
            Schema.AddTable("table_name", t =>
            {
                //id column is added automatically
                t.AddString("some_column");
            });
            */
            
        }

        public override void Down()
        {
            //just undo what you've done at Up()
            //Schema.RemoveTable("table_name");
        }
    }

}