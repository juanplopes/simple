<div class="confirm">
    @using (Html.BeginForm())
    {
        <span>
            @ViewBag.ConfirmMessage</span>
        <div class="buttons">
            <input type="submit" value="yes" />
            <a href="javascript:$.fancybox.close()" class="button">no</a>
        </div>
    }
</div>
