<div class="confirm">
    @using (Html.BeginForm())
    {
        <span>
            @ViewBag.ConfirmMessage</span>
        <div class="buttons">
            @Html.Submit("yes")
            <a href="javascript:$.fancybox.close()" class="button">no</a>
        </div>
    }
</div>
